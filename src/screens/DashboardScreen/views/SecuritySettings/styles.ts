import styled from 'styled-components'
import { View, ViewContent } from '@/utils/styles/common'

export const SecuritySettingsView = styled(View)`
  display: flex;
`

export const SecuritySettingsViewContent = styled(ViewContent)`
  display: flex;
`
