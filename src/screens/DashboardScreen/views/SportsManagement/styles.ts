import styled from 'styled-components'
import { View, ViewContent } from '@/utils/styles/common'

export const SportsManagementView = styled(View)`
  display: flex;
`

export const SportsManagementViewContent = styled(ViewContent)`
  display: flex;
`
