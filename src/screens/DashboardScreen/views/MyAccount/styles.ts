import styled from 'styled-components'
import { View, ViewContent } from '@/utils/styles/common'

export const MyAccountView = styled(View)`
  display: flex;
`

export const MyAccountViewContent = styled(ViewContent)`
  display: flex;
`
